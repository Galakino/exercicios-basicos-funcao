/*

1. Faça um programa que possua uma função que receba um valor inteiro e verifica se o valor é 
positivo ou negativo. A função deve retornar um se for positivo e zero se for negativo. Deve 
existir também um procedimento que faça a leitura. 

*/

#include <iostream>
#include <locale.h>
using namespace std;


int valor (int n){
    if(n < 0){
        return 0;
    } else {
        return 1;
    }
}

void leitura (int &number){
    cout<<"Digite um numero: ";
    cin>>number;
}

int main()
{
    int number;
    
    leitura(number);
    
    if(valor(number) == 1){
        cout<<"Numero é positivo";
    } else{
        cout<<"Numero é  negativo";
    }
    
    
    return 0;
}
